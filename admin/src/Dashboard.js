import * as React from "react";
import { Card, CardHeader } from '@material-ui/core';
import { Title } from 'react-admin';

const Dashboard = () => (
    <Card>
        <Title title="College Hub - Dashboard" />
        <CardHeader title="College Hub - Dashboard" />
    </Card>
);

export default Dashboard;